/**
 * Enhanced UX Components Index
 * 
 * React/Ink components for the split-screen CLI interface.
 * These components implement the UI layer for:
 * - Streaming output region (Claude responses)
 * - Approval workflow region (task decomposition and approval)
 * - Persistent input region (always-available input)
 * 
 * Phase 3.3: T030-T032
 */

// Export from types.ts until individual component files are created
export * from '../types.js';